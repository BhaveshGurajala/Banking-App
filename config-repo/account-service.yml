server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bankapp_account?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

jwt:
  secret: ThisIsADevOnlySecretKeyChangeMeInProdMinimum256BitsLong123

internal:
  api-key: a7f3d9e1-banking-internal-key-92c8b4